/**
 * Compat shim — the canonical ThemeToggle now lives in
 * `components/ui/theme-toggle.tsx`. Re-exported here so existing imports
 * (`./components/ThemeToggle`) keep working.
 */
export { ThemeToggle } from './ui/theme-toggle';
