# Formatting is enforced in CI (`cargo fmt --all --check`). Only stable rustfmt
# options live here so a stable toolchain produces the canonical layout.
edition = "2021"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
